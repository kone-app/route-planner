name: Trigger App to Send Mail

on:
  schedule:
    #- cron: '0 3 * * *'  # mail at 6 AM
    - cron: '*/5 * * * *'  # mail at Every 5 Min
  #workflow_dispatch:       # Optional manual trigger

jobs:
  call-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call API endpoint
        run: |
          curl -X GET "https://q5csqlule0.execute-api.us-east-1.amazonaws.com/prod/journeys?origin=Aalto-yliopisto&destination=Keilaniemi&arriveBy=20250911084500"
